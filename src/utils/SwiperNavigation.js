const NextButton = ({ next }) => (
  <button onClick={next} className="custom-next-button">
    Next
  </button>
);

const PrevButton = ({ prev }) => (
  <button onClick={prev} className="custom-prev-button">
    Prev
  </button>
);
